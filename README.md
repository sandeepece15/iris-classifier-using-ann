# Iris FastAPI UI

## Important
Place your trained `iris_model.keras` file in this folder.

Your uploaded training script shows that the model was saved as `iris_model.keras` and the scaler as `scaler.pkl`.

## Run

```bash
pip install -r requirements.txt
uvicorn app:app --reload
```

Open:

http://127.0.0.1:8000
